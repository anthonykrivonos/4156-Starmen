{
  "name": "upmed",
  "version": "1.0.0",
  "repository": "https://github.com/anthonykrivonos/4156-Starmen.git",
  "license": "MIT",
  "devDependencies": {
    "pre-commit": "^1.2.2"
  },
  "scripts": {
    "precommit": "cd upmed-web && yarn lint && yarn format && yarn test && yarn jest && yarn build && cd .."
  },
  "pre-commit": [
    "precommit"
  ]
}
